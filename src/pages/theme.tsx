// theme.js
import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#6200ea',
    },
    grey: {
      200: '#eeeeee',
      300: '#e0e0e0',
    },
  },
});

export default theme;
